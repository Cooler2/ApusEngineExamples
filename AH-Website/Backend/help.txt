ѕринцип использовани€ шаблонов:

√руз€тс€ все файлы из templates, каждый файл может содержать множество шаблонов.
 аждый шаблон начинаетс€ с имени в формате:
#NAME:
Ќе забываем про двоеточие!
≈сли после двоеточи€ есть какие-либо символы (кроме пробелов), то это однострочный шаблон
и только эти символы €вл€ютс€ его содержимым. ≈сли же символов нет, то шаблон многострочный -
его содержимое идЄт в последующих строках, а остаток строки с именем игнорируетс€.
≈сли содержимого нет нигде - шаблон пустой (так тоже можно, но не нужно).

—сылки на шаблоны:
“екст шаблона может содержать ссылки на другие шаблоны вида: $NAME
ѕри этом дл€ подстановки сначала провер€етс€ список временных значений, и только после
выполнени€ всех замен текст еще раз транслируетс€ уже с использованием глобальных шаблонов.
≈сли шаблона с заданным именем нет - ссылка замен€етс€ пустой строкой.
¬се трансл€ции выполн€ютс€ рекурсивно, т.е. сперва транслируетс€ текст вставл€емого шаблона,
а уже затем полученный результат вставл€етс€ в исходный текст.

—троки начинающиес€ с // - €вл€ютс€ комментари€ми и при загрузке игнорируютс€